package Thread;

public class VolatileKeyword {
	
		public static void main(String arg[]) {
		//static volatile int var =5;  
		} 
}

/*
 * In the above example, static variables are class members that are shared
 * among all objects. There is only one copy in the main memory. The value of a
 * volatile variable will never be stored in the cache. All read and write will
 * be done from and to the main memory
 */